apiVersion: apps/v1
kind: Deployment
metadata:
  name: server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: server
  template:
    metadata:
      labels:
        app: server
    spec:
      initContainers:
      - name: wait-for-mysql
        image: busybox:1.35
        command:
        - sh
        - -c
        - |
          until nc -z mysql 3306; do
            echo "Waiting for MySQL..."
            sleep 2
          done
          echo "MySQL is available!"
      containers:
        - name: server
          image: akk29/public:swippter-backend-server
          ports:
            - containerPort: 8000
          envFrom:
          - secretRef:
              name: otel-env-secret
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          volumeMounts:
            - name: static-volume
              mountPath: /app/swippter/static-files
      volumes:
        - name: static-volume
          persistentVolumeClaim:
            claimName: static-pvc